# 🔎 Habr Daily Parser

Парсер собирает топ-статьи за день с сайта [habr.com](https://habr.com) и сохраняет их в файл `articles.json`.

Каждая статья включает:
- заголовок;
- количество просмотров;
- ссылку на оригинал;
- текст статьи (без `script`, `style` и `code`-блоков).

---

## 🚀 Как использовать

1. Установить зависимости:

```bash
pip install requests bs4 fake-useragent lxml
```
Запустить парсер:
bash```
python main.py
```

После выполнения появится файл:

pgsql```
articles.json
```
📦 Пример структуры JSON
```json
[
  {
    "title": "Пример статьи",
    "views": "5.1K",
    "url": "https://habr.com/ru/articles/123456",
    "text": "Полный текст статьи..."
  }
]
```

